import { KpiColorPercentPipe } from './kpi-color-percent.pipe';

describe('KpiColorPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new KpiColorPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
